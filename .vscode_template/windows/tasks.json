{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "configure",
            "type": "shell",
            "command": "cmake -S . -B build -G \"Visual Studio 17 2022\" -DSMUFL_MAPPING_USE_PREGENERATED_HEADERS=OFF"
        },
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build build --config Release",
            "dependsOn": ["configure"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
